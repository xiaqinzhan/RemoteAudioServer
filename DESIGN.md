# DESIGN.md

## 气质与意象
- 深色科技风，意象锚点：一块悬停在夜空中的雷达控制台，发着冷光的终端显示器上跳动着数字生命信号。
- 基调沉稳、克制、信息密集，像专业音频机房 + 网络运维监控台的结合体，而非花哨的科幻皮肤。

## 配色方案
- 背景：接近纯黑的深蓝黑 `#0a0f1a` 到 `#0d1420`（径向渐变），模拟暗房/监控室。
- 面板：半透明深灰蓝卡片 `#111a2b`，1px 低亮描边 `rgba(80,140,255,0.14)`。
- 主强调色：青蓝「信号绿/青」`#22d3ee`~`#3b82f6`（渐变），用于在线状态灯、音量电平、激活态。
- 次级强调：琥珀 `#f59e0b` 用于录音中/警告态；警示红 `#ef4444` 用于离线/停止。
- 文字：主文本 `#e6edf7`，次级 `#8aa0bd`，弱化 `#5b6c85`。

## 字体排版
- 字体族：`ui-monospace, SFMono-Regular, Menlo, Consolas, "JetBrains Mono", monospace`（科技终端感）。
- 中文字体中文优先：`"PingFang SC", "Microsoft YaHei", sans-serif` 兜底。
- 节奏：标题字号偏大但克制；数据项用等宽小字号，信息密度高但不拥挤；刻度线/边框营造仪表盘感。

## 动效与交互
- 在线状态灯：绿水「脉动」呼吸动画（透明度闪烁），离线灰灯静止。
- 音量电平条：随 RMS 实时刷新，绿色→青色→琥珀渐变指示强度，transition 100ms。
- 卡片 hover：轻微上浮 + 描边提亮（柔和过渡 0.18s）。
- 所有动效克制，避免模糊/爆炸性效果，保持终端监视感。

## 设计禁忌
- 禁止使用明亮背景、纯白卡片、高饱和紫蓝渐变大色块。
- 禁止使用 ScriptProcessorNode（音频播放必须 AudioWorklet）。
- 禁止过度圆角/毛玻璃/发光阴影喧宾夺主；圆角统一 10px。
- 首次未模拟设备时页面应给出引导而非空空一片。